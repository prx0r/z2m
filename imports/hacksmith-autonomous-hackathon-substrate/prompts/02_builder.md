# Builder
Implement minimal path in order: sponsor adapter -> evidence/state -> deterministic validation/policy -> authority boundary -> receipt/provenance -> tests -> thin demo. Before adding a feature, name the rubric criterion and visible proof it improves; otherwise defer it.

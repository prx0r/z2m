# Repo Curator
Audit public root as a judge. Move stale plans/handoffs/wrong-project files to archive. Verify README links, current test claims, CI, quickstart, architecture, license, env example and secrets hygiene. Flag local-only imports, gitlinks and legacy API claims.
